(require 'mu4e)
(require 'evil-mu4e)

(setq
 mu4e-maildir "~/.mail/whitehead1415"
 mu4e-sent-folder "/sent"
 mu4e-drafts-folder "/drafts"
 mu4e-trash-folder "/trash"
 mu4e-refile-folder "/archive"
 mu4e-get-mail-command "mbsync -a"
 )
